<%= text_field :title, :size => 50, :label => 'Title:' %>
<%= text_area :content, :rows => 20, :cols => 60, :label => 'Content:' %> 
<br>
  <span class="mock_label">Formatter</span>
<%= select :formatter, :collection => Hooks::Formatters.available_formatters, :selected => (@sidebar_group.new_record? ? (self.current_user.default_formatter || "default") : @sidebar_group.formatter) %>
