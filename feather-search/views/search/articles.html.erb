<div id="search-results">
	<% if !@results.empty? %>
		<h3>Results:</h3>
	  <ul>
			<% @results.each do |a| %>
			<li><%= link_to(a.title, a.permalink) %></li>
			<% end %>
		</ul>
	<% elsif !@empty_query %>
	  <h3>No articles found for <%= params[:query] %>.</h3>
	<% end %>
</div>